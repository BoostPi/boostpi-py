name: Lint

on: [workflow_call]

jobs:
    lint:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: psf/black@stable
              with:
                options: "--check --diff"

